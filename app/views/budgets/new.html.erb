<section class="page group">
<%= render partial: 'layouts/overview_side_bar' %>
<section class="page-content">
  <header class="page-content-header">
    <h3 class="page-content-title">Create New Budget</h3>
  </header>

  <article class="page-body">
    <form action="<%= budgets_url %>" method="post" class="new-budget-form">
    <%= auth_token %>
    <div class="input">
    <label for="budget-amount">Amount</label>
    <input type="text" name="budget[amount]" id="budget-amount" value="<%= @budget.amount %>">
    </div>

    <div class="dropdowns group">
    <div class="dropdown">
    <label for="budget-category">Category</label>
    <select name="budget[category_id]" id="budget-category">
      <option></option>
      <% @categories.each do |category| %>
        <option value="<%= category.id %>" <%= "selected" if @budget.category_id == category.id %>><%= category.description %></option>
      <% end %>
    </select>
    </div>

    <div class="dropdown">
    <label for="budget-frequency">Frequency</label>
    <select name="budget[frequency]" id="budget-frequency">
      <option></option>
      <% FREQUENCIES.each do |val, description| %>
        <option value="<%= val %>"><%= description %></option>
      <% end %>
    </select>
    </div>

    <div class="dropdown">
    <label for="budget-reset-weekly">When to end period (weekly)?</label>
    <select name="budget[frequency_reset]" id="budget-reset-weekly">
      <option></option>
        <% WEEKLY_FREQUENCY_RESETS.each_with_index do |item, index| %>
        <option value="<%= index %>"><%= item %></option>
        <% end %>
    </select>
  </div>

    <div class="dropdown">
    <label for="budget-reset-monthly">When to end period (monthly)?</label>
    <select name="budget[frequency_reset]" id="budget-reset-monthly">
      <option></option>
        <% MONTHLY_FREQUENCY_RESETS.each_with_index do |item, index| %>
        <option value="<%= index %>"><%= item %></option>
        <% end %>
    </select>
  </div>
  </div>

  
    <div class="radio">
    <label for="budget-private">Private</label>
    <input type="radio" id="budget-private" name="budget[private]" value="true" <%= "checked" if (@budget.private == true) %>>

    <label for="budget-public">Public</label>
    <input type="radio" id="budget-public" name="budget[private]" value="false" <%= "checked" if (@budget.private == false) %>>
    </div>

    <div class="submit">
    <input type="submit" value="Create Budget!">
    </div>
    </form>


  </article>
</section>
</section>